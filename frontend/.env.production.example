# =============================================================================
# PET EYE AI — 프로덕션 프론트 빌드 변수
# Docker build-arg 또는 CI 에서 주입. Git 커밋 금지.
# =============================================================================

# 같은 도메인 nginx 프록시 사용 시 비워두면 /api 상대 경로 (권장)
# VITE_API_URL=

# nginx /ai 프록시 경로
VITE_AI_SERVER_URL=https://ganadi.site/ai

# 카카오 개발자 콘솔 JavaScript 키
VITE_KAKAO_MAP_KEY=
